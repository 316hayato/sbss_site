<%- if controller_name != 'sessions' %>
  <p>登録済みの方は<u><%= link_to "こちら", new_session_path(resource_name) %></u>からログインしてください。</p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p>会員登録がお済みでない方は<%= link_to "こちら", new_registration_path(resource_name) %></u>から新規登録を行ってください。
<% end %>

